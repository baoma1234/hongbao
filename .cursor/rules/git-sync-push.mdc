---
description: After code changes, commit and push to GitHub so the remote stays current
alwaysApply: true
---

# Git sync after updates

When finishing a batch of meaningful code changes in this project:

1. Create a git commit (concise message focused on why).
2. Push to `origin` (`https://github.com/baoma1234/hongbao.git`) on `main` with `git push -u origin HEAD` if needed.
3. Do **not** commit unrelated secrets: `.env`, `im-server/config/local.php`, DB passwords, zip dumps.
4. **`fanshub.php` 含 Telegram Token / webhook secret：要一并提交推送**（私有仓库；见 `no-push-telegram-secrets.mdc`）。
5. Do **not** amend unless the usual amend safety rules allow it.
6. Do **not** force-push to `main`.
7. After push, briefly tell the user the commit is on GitHub so the other server can `git pull`.

## `fanshub.php` 必须随 Git 同步

正式配置以本机 `application/extra/fanshub.php` 为准。

- 有业务配置改动或用户要求同步时：**本地与 GitHub 都要更新该文件**（其它服务器 `git pull`）
- 推送时保留 `telegram_bot_token` / `telegram_webhook_secret` 真实值（私有仓）
- 同时带上白名单、短信开关、文案等全部正式键

## SMS production flags（推送前必核）

`application/extra/fanshub.php` 推送到 GitHub / 生产时固定为：

- `sms_mock_enabled` => **false**（测试短信模式关闭）
- `sms_dagou_enabled` => **true**（大狗短信开启）
- `sms_una_enabled` => **true**（国际短信开启）

若本次改动或 diff 把上述三项改成了错误值，推送前改回再提交。本地临时开测试短信不要推上 `main`。

Skip empty commits when there are no changes.
