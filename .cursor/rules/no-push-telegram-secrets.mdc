---
description: fanshub.php Telegram bot config is synced to the private GitHub repo
alwaysApply: true
---

# Telegram / 机器人配置：随 fanshub.php 同步

本仓库为**私有仓库**。用户要求 `telegram_bot_token`、`telegram_webhook_secret`、`telegram_bot_username` 与正式 `application/extra/fanshub.php` **一并提交并推送**，方便各服务器 `git pull` 后直接可用。

## 做法

- 同步 / 提交 `fanshub.php` 时：**保留**本机真实 Token / webhook secret，不要置空
- 改机器人业务逻辑后正常提交；配置段随文件一起推
- 仍不要提交无关密钥：`.env`、`im-server/config/local.php`、数据库密码等

## 注意

- 若仓库将来改为公开，必须先轮换 Token 并改回「不进 Git」策略
- 其它环境 `git pull` 后如本地曾手改 Token，以仓库正式配置为准或自行合并
