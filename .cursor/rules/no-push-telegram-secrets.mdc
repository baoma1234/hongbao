---
description: Never commit or push Telegram bot tokens / local bot runtime secrets
alwaysApply: true
---

# Telegram / 机器人配置：禁止推送

线上 Telegram 机器人 Token、webhook secret、用户名等**已由运维在服务器本地配好**。

## 禁止

- 不要把 `telegram_bot_token`、`telegram_webhook_secret` 的真实值提交或推到 GitHub
- 不要为了「同步配置」去改并推送 `application/extra/fanshub.php` 里的 Telegram 段
- 仓库里这些字段应保持空字符串或占位；各环境各自本地填写

## 允许

- 改机器人**业务逻辑**代码（如 `FansHubTelegram.php`、WebApp 绑定页）后正常提交
- 用户**明确要求**更新线上 Token 时：只改**本机** `fanshub.php` 并执行 `set_telegram_bot_webhook.php`，**不 push** 含 Token 的 diff

## 与其它规则

推送 `fanshub.php` 时若必须带其它非密钥改动：提交前把 `telegram_bot_token` 置空（或还原为仓库占位），本机再写回真实 Token。
