# Applied schema / one-shot patches

These `patch_*.php` scripts were already run on production (or are obsolete i18n/HTML one-shots).
Keep for archaeology; do not re-run blindly.

Still-live ops patches stay in `scripts/` (e.g. `patch_secret_amount_decimal18.php`, niuniu/group RP patches).
