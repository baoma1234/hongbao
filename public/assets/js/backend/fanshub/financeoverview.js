define(['jquery', 'bootstrap', 'backend'], function ($, undefined, Backend) {
    var Controller = {
        index: function () {
            // 服务端渲染总览，无需表格初始化
        }
    };
    return Controller;
});
